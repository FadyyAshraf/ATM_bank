#ifndef UTIL_H
#define UTIL_H

// Function declarations for encryption, decryption, and helper functions
void encrypt_hill(const char* input, char* output);   // Encrypt using Hill cipher

#endif
